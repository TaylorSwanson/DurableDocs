{
  "name": "durabledocs",
  "version": "0.1.1",
  "description": "Simple document database wrapper on top of CloudFlare's global Durable Objects platform",
  "main": "dist/src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/TaylorSwanson/DurableDocs"
  },
  "scripts": {
    "start": "./start.sh",
    "build": "npx tsc && mv ./out/src ./dist && rm -rf ./out",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build"
  },
  "author": "Taylor Swanson",
  "license": "MPL-2.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.16.0",
    "typescript": "^4.8.4",
    "wrangler": "^2.1.9"
  }
}
